version: "3.8"
services:
  backend:
    build: .
    env_file: .env
    ports: ["8080:8080"]
  db:
    image: postgres:15
    environment: {POSTGRES_DB:budgetbuddy}
  ai-proxy:
    image: alpine
    command: ["sh","-c","sleep infinity"]